<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:fresco="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_base_layout_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:paddingBottom="65dp"
        android:layout_centerInParent="true"
        android:scaleType="centerInside"
        android:src="@drawable/ic_launcher" />



    <ImageView
        android:id="@+id/splash_ad_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:visibility="gone"
       />

    <LinearLayout
        android:id="@+id/show_duration_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_marginRight="21.5dp"
        android:layout_marginTop="40dp"
        android:padding="4dp"
        android:orientation="horizontal"
        android:visibility="gone"
        tools:visibility="visible">

        <TextView
            android:id="@+id/show_duration"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:layout_marginRight="5dp"
            android:background="@drawable/splash_left"
            android:gravity="center"
            android:textColor="#fff"
            android:textSize="15sp"
            tools:text="3" />

        <ImageView
            android:id="@+id/next"
            android:layout_width="27dp"
            android:layout_height="13dp"
            android:layout_gravity="center_vertical"
            android:layout_marginTop="1dp"
            android:background="@drawable/splash_next_icon"
            android:paddingBottom="5dp"
            android:paddingLeft="3dp"
            android:paddingTop="5dp"
            android:textColor="#fff" />
    </LinearLayout>
    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:paddingBottom="30dp"
        android:scaleType="centerInside"
        android:text="人人机-让人人买得起的好手机" />

</RelativeLayout>